import processing.core.PImage;

import java.util.List;

public class PickleRick extends Entity {
    public PickleRick(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
