import processing.core.PImage;

import java.util.List;

public class Alien extends Entity {
    public Alien(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
